{
  "name": "stream-cloud",
  "version": "1.0.0",
  "description": "SoundCloud with friends",
  "main": "index.js",
  "author": "StreamCloud",
  "license": "MIT",
  "dependencies": {
    "express": "^4.15.2",
    "socket.io": "^1.7.3"
  },
  "scripts": {
    "build": "babel client -d build",
    "start": "babel client -d build && nodemon server/index.js"
  },
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "nodemon": "^1.11.0"
  }
}
